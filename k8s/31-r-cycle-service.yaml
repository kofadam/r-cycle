apiVersion: v1
kind: Service
metadata:
  name: r-cycle
  namespace: r-cycle
  labels:
    app: r-cycle
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: r-cycle
